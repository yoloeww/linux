#include <iostream>
#include <unistd.h>
#include <signal.h>
using namespace std;
static vector<int> sigarr = {2};

static void show_pending(const sigset_t &pending) {
    
}
int main() {
    return 0;
}
